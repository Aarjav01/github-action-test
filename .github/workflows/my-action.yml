name: My First Action  # Visible name in Actions tab

on: [push]  # Triggers on any push to the repo

jobs:
  test:
    runs-on: ubuntu-latest  # Free GitHub runner (virtual machine)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Reusable action to grab your repo files

      - name: 23f1001127@ds.study.iitm.ac.in  # Your required step!
        run: echo "Hello from my custom action step!"

      - name: List files
        run: ls -la  # Simple demo task
